{
  "name": "demo",
  "version": "1.0.0",
  "description": "coding challenge for OpenSponsorship",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon ./server.js",
    "build": "cd frontend && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Savannah Amos",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.18"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
